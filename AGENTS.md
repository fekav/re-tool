# AGENTS.md

## Core Rules

- If a task matches a skill, ask every time for confirmation before you invoke it.
- When `test-driven-development` is invoked and it involves writing, modifying, or reviewing Java tests, invoke and follow `java-junit-testing` exactly before changing those tests.
- Use `domain-driven-design-coder` when creating new slices, or when inoked explicitly.